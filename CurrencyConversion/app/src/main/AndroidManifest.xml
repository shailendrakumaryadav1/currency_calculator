<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		package="com.currency.currencyconversion">

	<application
			android:allowBackup="true"
			android:icon="@mipmap/ic_lcon"
			android:label="@string/app_name"
			android:supportsRtl="true"
			android:theme="@style/AppTheme"
			android:name="currency.mastercard.ThisApplication">
		<activity
				android:name="currency.mastercard.ui.CurrencyActivity"
				android:label="@string/app_name"
				android:screenOrientation="portrait">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<activity
				android:name="currency.mastercard.ui.CurrencySelectionActivity"
				android:label="@string/text_currency_selection_activity_title"
				android:screenOrientation="portrait">
			<!--Default Intent Filter-->
			<intent-filter>
				<action android:name="android.intent.action.DEFAULT" />
			</intent-filter>
		</activity>

	</application>

	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>